#include "monty.h"
/**
 * f_add -  adds the top two elements of the stack.
 * @head: the top of the stackk
 * @line_number: the line number
 * Return: nothing
 */
void f_add(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
}
