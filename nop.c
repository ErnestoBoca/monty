#include "monty.h"
/**
 * f_nop - doesn’t do anything.
 * @head: the top os the stack
 * @line_number: the line number
 * Return: nothing
 */
void f_nop(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
}
